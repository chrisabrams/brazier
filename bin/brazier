#!/usr/bin/env node

require('6to5/register')
require('6to5/polyfill')

var argv           = require('minimist')(process.argv.slice(2)),
    BootController = require('./boot'),
    pkg            = require('../package.json'),
    routes         = require('./routes')

var boot = new BootController({
  argv   : argv,
  pkg    : pkg,
  routes : routes
})
